@model BookStore.Models.ViewModels.AuthorViewModel
@{
    ViewData["Title"] = "Добавление нового автора";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="card">
    <div class="card-header">
        <h1>Добавление нового автора</h1>
    </div>
    <div class="card-body">
        <form asp-controller="AdminAuthor" asp-action="Create" method="post">
            <div class="mb-3">
                <label asp-for="Name" class="form-label">ФИО</label>
                <input type="text" class="form-control" asp-for="Name">
            </div>
            <button type="submit" class="btn btn-primary">Сохранить</button>
        </form>
    </div>
</div>

